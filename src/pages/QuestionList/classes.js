import { makeStyles } from "@material-ui/core/styles";

// styles
export default makeStyles((theme) => ({
  skeletion: {
    margin: "0.5rem auto",
  },
  pagination: {
    margin: "1rem",
    display: "flex",
    justifyContent: "center",
  },
}));
