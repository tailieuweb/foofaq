import { makeStyles } from "@material-ui/core/styles";

// styles
export default makeStyles((theme) => ({
  skeletion: {
    margin: "1rem auto",
  },
  pagination: {
    margin: "1rem",
    display: "flex",
    justifyContent: "center",
  },
}));
